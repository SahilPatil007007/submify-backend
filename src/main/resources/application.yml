spring:
  application:
    name: submify-backend

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://db.dbwoifhjphzpemwqaunj.supabase.co:5432/postgres
    username: postgres
    password: SahilProject@123

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        format_sql: true
